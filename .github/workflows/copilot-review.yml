name: Copilot Review

on:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review]

permissions: {}

jobs:
  review:
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pull-requests: write

    steps:
      - name: Request and collect Copilot review
        uses: themoretheless/.github/.github/actions/copilot-review@d66cd190a0d7a8b45f909dfa97597be34ec93f1f # v1
        with:
          github-token: ${{ github.token }}
          # Copilot always emits an overview body; gate only actionable inline comments.
          fail-on-comments: true
