# Decision gates for batch 251-300

Reviewed on 2026-07-21. These gates keep everyday workflow helpers, device policy, and integration contracts from being reported as native or networked product behavior before their final adapters exist.

| Backlog | Gate | Continue rule | Blocked claim / fallback | Required evidence |
|---:|---|---|---|---|
| 251 | Continuous auto-pause | Native adapters continuously publish bounded idle, screen-lock, and remote-control signals; manual pause always wins; resume never occurs while a blocking signal remains. | Claim startup remote-session pause and tested policy only. Keep the visible manual pause action. | Per-OS lock/unlock, idle/wake, remote attach/detach tests plus no-capture assertions while paused. |
| 252, 258, 262 | Learned everyday behavior | Suggestions are derived locally from bounded counters, expose the exact proposed rule, require confirmation, and retain dismissal without content telemetry. | Keep rule suggestions, burst grouping, and clean-link memory as process-local foundations. | Runtime UX tests, persistence/privacy review, false-positive corpus, and reset controls. |
| 266-280 | Device experience | Authenticated discovery/transport, durable encrypted state, verified device identity, replay defense, revocation acknowledgement, and two-device replication all pass the sync threat model. | Describe trust/rehearsal/replay/outbox/QR/approval code as transport-independent policy; do not claim working sync or handoff. | Two real devices across online/offline/revoke/conflict/travel-mode cases, content-free audit receipts, and packet/residue review. |
| 276-277 | Handoff and nearby send | A one-time token is bound to an authenticated local transfer, has one recipient and expiry, and never places clipboard bytes in discovery or QR metadata. | The current QR value is a one-time token only; nearby targets are verified records, not discovered devices. | Replay/expiry tests, packet capture, UI confirmation, wrong-target denial, and offline failure behavior. |
| 281-295 | External integrations | A signed client reaches authenticated daemon dispatch through exact capability scope, bounded messages, revocation, and content-free diagnostics. | Keep browser/editor/Vim/automation/MCP/launcher/OSC52/webhook/adapters as library contracts; expose no listener or extension download. | Client conformance suites, hostile-input tests, install/revoke receipts, daemon authorization tests, and native UX review. |
| 286-288, 291, 295 | Remote/API authority | One-shot or scoped credentials are authenticated, expiring, replay-protected, destination-bound, and denied for sensitive content unless policy explicitly permits it. | No remote paste endpoint, webhook listener, or MCP server is advertised. | End-to-end replay, clock skew, token theft, revocation, destination confusion, and redacted-log tests. |
| 297 | Release evidence | Every release run publishes the three-OS test logs, bounded residue-canary scope, dependency audit/deny/vet logs, measured performance output, SBOM, checksums, reproducibility result, and provenance under one source-bound manifest. | Do not interpret residue-deletion canaries as proof of whole-database encryption; [LIM-001](limitations.md) remains open. | Successful tag workflow, downloaded manifest verification, independent binary comparison, and SQLCipher canary only after that gate lands. |
| 298 | Config migration | Preview parses a bounded owner-local file, reports only field-level changes, rejects newer/invalid schemas, and apply creates an owner-only rollback copy before atomic replacement. | Schema 1 is never silently rewritten by an ordinary save. Refuse mutation and direct the user to preview/apply. | Schema 1 fixture, malformed/negative/oversized/newer-version tests, mode checks, rollback comparison, and interrupted-write test. |
| 299-300 | Governance | Two maintainers can execute the handoff drill; quarterly reviews record explicit Promote/Keep/Defer/Cut decisions and preserve the canonical scope boundary. | Documentation and reminders are process evidence, not proof that credentials or backup maintainers exist. Missing custodians remain release blockers. | Completed handoff drill, repository access audit, dated scope record, and one successful scheduled/dispatch reminder. |

Missing external evidence remains `Unknown`. The current executable stays local and honest about foundations even when the underlying contracts are fully tested.
