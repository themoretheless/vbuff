# Decision gates for batch 301-350

Reviewed on 2026-07-21. These gates prevent privacy helpers, recall sidecars, schema 7 APIs, and generic desktop probes from being advertised as complete user-facing or native behavior before their final owners and evidence exist.

| Backlog | Gate | Continue rule | Blocked claim / fallback | Required evidence |
|---:|---|---|---|---|
| 301, 304, 309-313 | Trust-policy activation | One capture/external-action dispatcher owns every decision; state is owner-local, bounded, content-free, and tested through restart, denial, expiry, and reset. | Call these tested foundations; keep current capture defaults and do not claim consent, strict workspaces, rule analytics, reason prompts, panic sealing, or device unlock UI. | End-to-end runtime tests, persistence migration, redacted-log/residue audit, denial-path test, and user-visible reset/export controls. |
| 302, 305, 315 | Sensitive presentation | Every detected reason survives capture/store/load, sensitive text never enters search/thumbnail/log paths, and expiry remains enforceable while the popup is hidden or asleep. | Use full masking plus two-second Peek; do not expose partial card/token digits merely because a mask contract exists. | Golden UI states, screen-reader names, restart/expiry tests, canary search/log checks, and shoulder-surfing review. |
| 306, 308, 312 | Security artifact custody | Export/update/crash formats are versioned and authenticated; signing/encryption keys come from reviewed release or OS-keystore custody with rotation and rollback procedures. | No remote detector updater or automatic crash writer; policy export remains an explicit library function. | Compromise/rollback/expiry tests, key rotation drill, filesystem-mode checks, reproducible fixtures, and independent verification. |
| 307 | Clipboard replacement quarantine | Paste injection is one-shot, destination-bound where possible, re-verifies the exact clipboard immediately before input, and never auto-renders the replacement. | Block and report copy-only/error; inspection must be an explicit local action. | Native race corpus, attacker replacement tests, focus-change tests, unreadable clipboard case, and content-free logs. |
| 314 | Private browsing authority | A signed authenticated browser client supplies a freshness-bound private-context assertion; missing/stale/unknown context is NeverStore. | Describe policy only; the generic OS clipboard cannot restore private-window provenance. | Chrome/Firefox/Safari client tests, forged/stale report rejection, uninstall behavior, and packet/log review. |
| 316-329 | Recall memory and provenance | Sidecars are persisted under one annotation/query owner, sensitive content/queries are excluded, ranking remains deterministic, and every UI action is backed by a real source. | Natural parsing, explanations, typo recovery, completions, and basic empty recovery may run now; macros, aliases, affinity, history, lock, query pins, graph, and find-source remain foundations. | Multilingual/timezone corpus, ranking goldens, encrypted persistence, migration/reset controls, source-open sandbox tests, and p99 budget. |
| 331-345 | Schema 7 lifecycle activation | Every destructive operation has preview, transaction/resume semantics, legal-hold/privacy precedence, bounded work, and a verified data-contract fixture. | Keep archive/collection/restore/maintenance APIs out of the main UX until transactions and operator feedback are complete. | Schema 6-to-7 fixture, downgrade round trip, fault injection, CAS corruption/repair corpus, forecast-vs-result metrics, and UI confirmation tests. |
| 335, 344 | Portable export compatibility | Export versions have public fixtures, deterministic canonical fields, explicit loss reports, and successful import by each claimed older consumer. | JSON v1/v2 library envelopes only; no claim of SQLite compatibility or lossless downgrade. | Cross-version binaries, malformed/oversized fixture rejection, round-trip hash checks, and documented removed fields. |
| 338, 342 | Audit evidence semantics | Residency flags are written atomically with the event they attest to; backup freshness advances only after checksum verification and durable close. | Existing sidecars are foundations; do not infer memory-only or verified backup from absence/presence alone. | Crash/fault injection around export/sync/backup commit, monotonicity tests, and independent checksum restore. |
| 346-350 | Native desktop truth | Each OS/compositor proves menu/tray states, commands, accessibility names, permissions, and failure transitions on real sessions; unsupported capability remains unavailable. | macOS/Windows/Linux code paths compile and generic Wayland stays copy-only; Linux falls back directly to the second-launch popup when SNI fails. | macOS menu-bar, Windows notification-area, GNOME/KDE/wlroots SNI/portal, lock/pause/paste timing, DPI/theme, VoiceOver/Narrator/AT-SPI, and UIPI tests. |
| 348 | Linux fallback hierarchy | A legacy adapter is independently implemented and selected only after SNI registration failure; popup command remains available regardless of tray support. | Do not claim the legacy middle rung today; use SNI then proven single-instance popup. | Real desktop matrix showing SNI success/failure, legacy ownership, duplicate-launch popup, restart, and no duplicate resident process. |
| 349-350 | Permission/capability repair | Probe results have a timestamp/source, changes are watched, automatic paste is disabled immediately on loss, and repair actions point to a relevant native control. | Capability rows are fail-closed snapshots; Windows has no universal permission toggle and Wayland has no generic green state. | Permission revoke/grant while running, portal disappearance, remote-session transition, elevated Windows target, and recovery without restart. |

Missing external evidence remains `Unknown`. The current executable stays local and copy-only wherever a native capability cannot be proved.
