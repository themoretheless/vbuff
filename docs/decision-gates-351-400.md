# Decision gates for batch 351-400

Reviewed on 2026-07-28. These gates keep desktop policy, team contracts,
local-automation schemas, plugin governance, and release policy from being
advertised as active features before their runtime owners and external evidence
exist.

| Backlog | Gate | Continue rule | Blocked claim / fallback | Required evidence |
|---:|---|---|---|---|
| 351-360 | Desktop policy activation | One root configuration owner persists the selected access mode, layout-aware shortcut, portable/managed policy, repair state, and native theme observation. Native registration/probes remain authoritative. | Treat `desktop_policy` as a pure foundation; current resident defaults and generic capability rows remain unchanged. | macOS/Windows/GNOME/KDE/Sway/Hyprland/X11 registration, restart, conflict, mixed-DPI, theme, and assistive-technology tests. |
| 356-357 | Portable and managed profile safety | Machine policy location, ownership, precedence, schema, signature/administrator trust, and profile filesystem permissions are defined per OS. | No machine-wide policy reader or portable-mode switch is active. | Clean-install/upgrade tests, unprivileged tampering denial, path traversal/symlink cases, removable-media failure, and backup/export behavior. |
| 361-375 | Team transport and authority | Authenticated membership, durable role/policy state, E2EE transport, revocation, expiry, receipts, audit retention, and owner recovery work across two real devices. | Team types remain disconnected contracts; no shared clip, external link, broadcast, admin audit, or policy simulation is advertised. | Threat model, two-device fixtures, offline/replay/conflict tests, cryptographic link revocation, member removal, metadata-residue audit, and UI/accessibility review. |
| 365 | External share | The item is encrypted to a scoped recipient/link key, the server cannot decrypt it, expiry and revocation are enforced online and after cache refresh, and access never expands history scope. | `ExternalShareGrant` records intent only; no URL or transport exists. | Cryptographic protocol review, revoked/offline cache tests, expiry clock-skew cases, abuse limits, and server/log inspection. |
| 376-390 | Daemon and CLI activation | M7 approves a local daemon boundary; every session authenticates, negotiates schema, limits frames/rates, binds locally, and routes all mutations through preview plus explicit apply. | IPC/CLI structures are foundations only; the resident binary opens no RPC/webhook listener and launches no headless command path. | Old/new binary fixtures, UDS/named-pipe permissions, loopback rebinding/token tests, crash/replay tests, shell integration, and end-to-end dry-run proof. |
| 380-382, 387-388 | Plugin execution and fetch sandbox | A subprocess host enforces per-action capabilities outside the plugin process, verifies bundles/publisher trust, limits memory/CPU/output/network, and isolates one crash from capture/store. | Governance validates manifests/evidence only; no plugin executable or network request is started. | OS sandbox profiles, hostile-plugin corpus, DNS rebinding/redirect/credential tests, signature rotation/revocation, marketplace review, and panic/resource-exhaustion recovery. |
| 383 | Loopback webhook | Listener binds loopback only, authenticates exact-scope rotating tokens, limits body/rate/concurrency, rejects browser cross-origin abuse, and emits content-free logs. | Endpoint validation exists without a listener. | IPv4/IPv6 bind proof, DNS rebinding, CSRF/CORS, token replay/rotation, oversized body, slow-client, and shutdown tests. |
| 384-386 | Operations truth | Backup closes durably and verifies restore; health remains payload-free; fixture export strips content and risky source metadata before user review. | Plans/manifests are foundations and must not be labeled a completed backup, endpoint, or fixture export. | Fault-injected write/restore, checksum and key loss, canary residue scan, schema fixtures, and explicit user confirmation. |
| 389-390 | Mutation containment | Every token and mutation class has one central limiter; dry-run and apply share validation and differ only at the final commit boundary. | Reject unpreviewed headless mutation and rate-limited requests; no side effects on dry-run. | Concurrent/restart/clock-window tests, transactional rollback, exact preview/apply diff, and audit records without payload. |
| 391 | Account-free voting privacy | Opaque short-lived tokens, aggregate-only storage, minimal infrastructure retention, abuse controls, and public protocol pass a privacy review. | Use public issues and scope review; no hosted vote service or stable device id. | Data-flow diagram, retention verification, replay/stuffing tests, thresholded public output, disable drill, and independent privacy review. |
| 392-394 | Public evidence and feedback | Every comparison cell is dated and reproducible, dogfood intervals actually run, and the report preview shows the exact outgoing content before an explicit draft action. | Historical research stays research; unpublished dogfood is not evidence; raw logs/databases/screenshots remain excluded. | Reproduction receipts, stale-row automation, 14-day diary, redaction canary corpus, size bounds, and user-review UI tests. |
| 395-397 | Release/LTS/security operations | Two custodians, signed artifacts, recovery drill, supported-version declaration, private disclosure channel, advisory dry run, and staffing budget exist. | No active LTS or fixed pre-1.0 train is claimed; security targets are policy until exercised. | Signed release rehearsal, credential recovery, private-report drill, CVE/advisory exercise, backport matrix, notification template, and support-window owner. |
| 398 | Portability promise | A documented offline export and verified import/restore remain free of account, entitlement, and network dependencies for every supported release. | Current library JSON is not called a complete backup or SQLite export. | Cross-version fixtures, entitlement-offline test, clean-machine restore, loss report, final-release/sunset drill, and public schema docs. |
| 399-400 | Cutline and entropy | Quarterly automation opens one review, the checked-in record assigns Now/Next/Later/Never, and each 25 additions balances at least 10 merges/prunes/demotions. | New candidates remain outside the active objective until accounting and prerequisite gates pass. | Workflow run, linked issue, dated decision record, backlog diff accounting, and CI/document contract review. |

Missing runtime, native, hosted-service, support, or maintainer evidence remains
`Unknown`. A checked-in contract is useful implementation evidence, but it is
not proof that a listener, network service, team feature, plugin host, LTS
channel, or native desktop behavior is active.
