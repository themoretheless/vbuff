# Implementation batch 101-150

Reviewed on 2026-07-18. This ledger is the execution overlay for engineering backlog items 101-150 in [architecture.md](../architecture.md) and [recommendation.md](../recommendation.md). It distinguishes resident behavior from tested contracts and records where native APIs, real installers, live sync, legal ownership, or a hosted service are still required.

## Status vocabulary

| Status | Meaning |
|---|---|
| **Runtime** | Exercised by the current resident binary, CLI, store, packaging, or CI path. |
| **Foundation** | Implemented and tested as a reusable contract or algorithm, but not connected to its final runtime. |
| **Adapted** | The proposal was narrowed to preserve safety, privacy, or an honest product boundary. |
| **Native required** | A model, fallback, or gate exists, but completion depends on real per-OS APIs or release infrastructure. |
| **Rejected** | The mechanism conflicts with a security, licensing, or correctness constraint; the replacement is recorded. |

## Item ledger

| Item | Status | Landed evidence | Remaining product work |
|---:|---|---|---|
| 101 | Foundation | [`CallbackInvocation`](../crates/vbuff-ipc/src/callback.rs) parses a bounded action URI, permits only reviewed callback schemes, rejects duplicate parameters, and verifies short-lived one-shot action-bound tokens. | Register the OS URL scheme and dispatch approved results through the live daemon without placing clipboard payloads in URLs. |
| 102 | Runtime | [`cf_html.rs`](../crates/vbuff-platform/src/cf_html.rs) and [`format_map.rs`](../crates/vbuff-platform/src/format_map.rs) are bounded parsers with checked-in fuzz corpora and scheduled libFuzzer targets alongside flavor JSON. | Add sanitized real-world corpus cases and upstream the targets to OSS-Fuzz for continuous campaigns. |
| 103 | Runtime | [`popup_snapshots.rs`](../crates/vbuff-gui/tests/popup_snapshots.rs) renders 12 golden PNGs across light/dark, 1x/2x, empty/populated/Trust states on one pinned Linux Mesa/Vulkan renderer. | Add 1.5x, CJK, bidi/RTL, long-source, and separate semantic platform-font lanes when those text paths land. |
| 104 | Runtime | [`release-provenance.yml`](../.github/workflows/release-provenance.yml) builds on two independent clean runners with locked inputs, normalized paths/time, and byte-compares artifacts before checksums/provenance. | Extend reproducibility to final macOS/Windows/Linux package containers and publish third-party rebuild attestations. |
| 105 | Foundation | [`vbuff-update`](../crates/vbuff-update/src/lib.rs) verifies signed manifests, monotonic sequences, minimum clients, wrong keys, replay/downgrade, key rotation, and deterministic staged rollout. | Add HTTPS fetch, durable accepted-sequence state, opt-in health criteria, installation, rollback-safe replacement, and updater UI. |
| 106 | Native required | [`quality.yml`](../.github/workflows/quality.yml) runs explicit X11/Wayland capability-model lanes and [`wayland.rs`](../crates/vbuff-platform/src/wayland.rs) tests protocol/paste ladders. | Run actual nested Mutter, KWin, and wlroots capture/paste conformance images; the environment-model lane is not compositor integration. |
| 107 | Foundation | [`PasteFidelityHarness`](../crates/vbuff-platform/src/paste_fidelity.rs) binds source/sink hashes and an exact modifier trace, detecting literal-character and modifier corruption. | Drive CGEvent, SendInput, XTEST, portal/libei, and real focused sink windows on each OS. |
| 108 | Adapted | [`packaging-smoke.yml`](../.github/workflows/packaging-smoke.yml) performs a clean `cargo install` on macOS, Windows, and Linux, then runs the installed checksum verifier and doctor JSON contract. | Feed actual DMG/PKG, MSI/MSIX, deb/rpm/Flatpak/AppImage artifacts into pristine images with no Rust toolchain and assert tray/single-instance behavior. |
| 109 | Native required | [`macos-notarize.yml`](../.github/workflows/macos-notarize.yml), [`package-macos.sh`](../scripts/package-macos.sh), and [`Info.plist`](../packaging/macos/Info.plist) define hardened signing, notarization, stapling, `codesign`, and Gatekeeper gates. | Configure protected Apple credentials, finalize entitlements/icon/version injection, and exercise the tagged release path against a distributed app bundle/DMG. |
| 110 | Runtime | [`mutation.yml`](../.github/workflows/mutation.yml) schedules pinned cargo-mutants matrices for core, platform, IPC, sync, and updater contracts and retains reports. | Establish reviewed kill-rate budgets and triage surviving mutants before making the scheduled job a required release gate. |
| 111 | Runtime | [`security.yml`](../.github/workflows/security.yml) gates deny/audit/vet/fuzz on PRs; release CI emits CycloneDX, checksums, and GitHub provenance only after reproducibility succeeds. | Retire temporary advisory exceptions and blanket vet exemptions through upstream audits; sign the SBOM itself. |
| 112 | Foundation | [`Clock`](../crates/vbuff-core/src/clock.rs), `SystemClock`, `ManualClock`, and exact deadlines are reusable; [`sync/clock.rs`](../crates/vbuff-sync/src/clock.rs) accepts injected time. | Inject the clock through resident retention/expiry and every native timer instead of constructing wall time at call sites. |
| 113 | Native required | [`zero_loss_capture.rs`](../crates/vbuff-core/tests/zero_loss_capture.rs) makes 50,000 consecutive generations and zero accounted loss a hard CI assertion. | Replace the deterministic generation model with per-OS multi-process clipboard-edge/store integration and latency budgets. |
| 114 | Runtime | [`PrivacyLedger`](../crates/vbuff-core/src/privacy.rs) hash-chains bounded content-free decisions; [`Diagnostics`](../src/diagnostics.rs) publishes verified entries to the live Trust surface. | Persist a separately keyed audit chain if users opt in and bind cipher-state evidence after SQLCipher lands. |
| 115 | Foundation | [`FormatFidelityReport`](../crates/vbuff-core/src/format_fidelity.rs) compares each offered flavor by byte evidence and reports lossless/degraded/missing without exposing content. | Run background capture-store-serve probes per backend/app and attach the measured badge to each GUI row. |
| 116 | Foundation | [`keymap.rs`](../crates/vbuff-platform/src/keymap.rs) defines one action set and proves parity across macOS, Windows, X11, and Wayland with native modifier labels. | Route every live frontend through the canonical map and publish an in-app behavioral parity report from real OS tests. |
| 117 | Foundation | [`PortableVault`](../crates/vbuff-sync/src/vault_export.rs) uses a bounded binary envelope, encrypts byte-identical clip records with XChaCha20-Poly1305, and rejects truncation, trailing bytes, bad magic, wrong keys, and tampering. | Specify the envelope publicly, add passphrase KDF/recipient wrapping, streaming I/O, CLI UX, and cross-version fixtures. |
| 118 | Runtime | The popup's shield action opens a first-class [`Trust and privacy`](../crates/vbuff-gui/src/app.rs) surface with detailed active/degraded/unavailable capability evidence. | React live to native permission/session changes and add actionable settings links without claiming unavailable controls. |
| 119 | Foundation | [`LiveMigrationTracker`](../crates/vbuff-plugin/src/migration.rs) accepts monotonic source revisions and produces bounded redacted migration state. | Implement read-only competitor adapters, file/watch semantics, deduplication, coexistence safeguards, and transactional ingestion. |
| 120 | Foundation | [`resolve`](../crates/vbuff-sync/src/conflict.rs) produces a deterministic, inspectable winner/loser/tie-break report and preserves a keep-both path. | Persist/edit logical clip operations, sync the conflict record, and build the merge UI over a live transport. |
| 121 | Foundation | [`CustodyChain`](../crates/vbuff-sync/src/provenance.rs) signs redacted device hops, detects tampering, and flags sensitive travel. | Bind device keys to membership, persist/filter custody records, and feed actual replication acknowledgements. |
| 122 | Foundation | Domain-separated signed [`BuildAttestation`](../crates/vbuff-update/src/attestation.rs), independent-build CI, and the offline verifier bind key id, source, builder, and artifact hashes. | Verify a downloaded transparency/provenance envelope against a pinned release identity from an About action. |
| 123 | Foundation | [`BurnClip`](../crates/vbuff-sync/src/burn.rs) permits one paste and requires signed per-device wipe receipts before reporting all-device destruction. | Integrate secure store/WAL deletion, epoch revocation, offline-device timeout policy, UI, and live device acknowledgements. |
| 124 | Foundation | [`derive_isolated_collection_key`](../crates/vbuff-sync/src/collection_vault.rs) separates root, unlock secret, collection id, and epoch; locked vault policy already denies read/sync. | Store rows under the derived key and build unlock/search/rekey/recovery UI after encrypted storage exists. |
| 125 | Runtime | The Trust surface shows explicit release SLO rows; zero-loss starts `Unknown`, becomes `Met` only after accounting evidence, and turns red on a breach, while uninstrumented search/CPU/login metrics never become silent green. | Publish measured p99/CPU/login samples from controlled runtime and CI probes with versioned host-normalized budgets. |
| 126 | Foundation | [`OfflineRunEvidence`](../crates/vbuff-plugin/src/offline.rs) issues a signed badge only when the manifest has no network grant and the host observed zero network calls. | Add a local model runtime, enforce network denial below host calls, and expose model/source/resource evidence in the GUI. |
| 127 | Foundation | [`ClipboardTripwire`](../crates/vbuff-platform/src/tripwire.rs) detects repeated external reads with bounded storage, rollback-safe time windows, rate limits, and self-alert suppression. | Obtain trustworthy reader identity through native telemetry where legally and technically available; otherwise keep the capability unavailable. |
| 128 | Rejected | The [strategy decision record](product-strategy-decisions.md) keeps the repository `MIT OR Apache-2.0`. | Reconsider only through an explicit licensing governance process; no BSL conversion is planned. |
| 129 | Rejected | The decision record refuses a legal/escrow promise without an operator, trigger, custodian, and key plan. | A future legal entity may propose a concrete third-party escrow agreement for separate review. |
| 130 | Adapted | The policy guarantees no recurring entitlement check can disable local releases; exact commercial terms are intentionally absent. | Pricing requires a seller, support/update cost evidence, refund policy, and jurisdiction review. |
| 131 | Adapted | Recurring fees are bounded to a future operated hosted service; no local feature or code path is paywalled. | Decide only alongside a working zero-knowledge relay and self-host parity. |
| 132 | Adapted | The future self-hosted relay path is recorded as subscription-free. | Implement and security-review the relay before making availability or compatibility claims. |
| 133 | Rejected | Ciphertext-byte metering was rejected because traffic volume remains behavioral metadata and creates the wrong collection incentive. | Prefer coarse fixed service limits if a hosted relay is ever offered. |
| 134 | Rejected | Scarcity and permanent founding-price promises were rejected before a product/billing entity exists. | Keep any future price history public and versioned instead of manufacturing urgency now. |
| 135 | Rejected | Per-library billing was rejected before encrypted collaboration semantics exist because it would shape server-visible metadata. | Revisit team packaging only after the protocol proves what the server cannot learn. |
| 136 | Rejected | Official signed/notarized artifacts and verification remain security controls, not a paid trust gate. | Funding may cover convenience/support, but cannot weaken free artifact verification. |
| 137 | Adapted | A future public security bounty is supported in principle, without an unfunded revenue-percentage promise. | Establish disclosure handling, scope, triage SLA, budget custody, and payout terms first. |
| 138 | Adapted | Public feature sponsorship may fund open work but cannot buy acceptance or bypass architecture/security review. | Define governance only when maintainers can operate it. |
| 139 | Adapted | Redacted shareable config is the narrow current foundation; no enterprise control plane or content audit exists. | Require SQLCipher, signed policy bundles, admin/user threat modeling, offline enforcement, and recovery semantics. |
| 140 | Adapted | Unlimited local history is kept outside commercial entitlement; hosted-device tiers remain deliberately undecided. | Base any future tier on measured relay cost and privacy impact, never local data volume. |
| 141 | Foundation | [`MigrationPreflight`](../crates/vbuff-plugin/src/migration.rs) reports clip/pin/snippet/image/likely-secret/byte/kind/unsupported counts without retaining content. | Populate it from real read-only competitor adapters and compare represented/imported counts before commit. |
| 142 | Runtime | The opt-in Developer starter pack in [`seed_pack.rs`](../src/seed_pack.rs) uses the same format/test/doctor commands documented by the project. | Add signed website deep links only after callback dispatch and pack installation are live. |
| 143 | Runtime | A fresh empty popup offers compact Developer/Writing seed choices; installation is explicit, local, bounded, and refreshes the real store once. | Add one-click removal/tagging and localized packs without pre-populating history silently. |
| 144 | Foundation | [`PermissionWatchdog`](../crates/vbuff-platform/src/permission.rs) debounces transient failures, emits one loss event with a settings deep link, and verifies restoration. | Implement Accessibility/portal probes and open exact native settings panes from the resident runtime. |
| 145 | Foundation | [`ImportBatchJournal`](../crates/vbuff-plugin/src/migration.rs) validates one import batch and produces a deterministic reverse-order rollback plan. | Execute adapter writes and rollback in one store transaction with durable undo metadata and GUI confirmation. |
| 146 | Foundation | [`SignedSnippetPack`](../crates/vbuff-plugin/src/snippet_pack.rs) is deterministic human-readable TOML, optionally domain-separated Ed25519-signed, rejects edited content, duplicate triggers, and oversized packs. | Add bounded file/HTTPS install, publisher trust/revocation, diff preview, transactional import, and CLI/UI routing. |
| 147 | Runtime | `vbuff config export [file]` emits a strict, history-free TOML that omits app/source matchers; bounded `config apply <file|->` validates limits, rejects ambiguous arguments, and preserves private local rules. | Add a GUI review/diff and explicit clipboard adapter; stdin already supports `pbpaste`/`wl-paste`/PowerShell pipelines. |
| 148 | Foundation | [`OnboardingState`](../crates/vbuff-core/src/onboarding.rs) produces contextual, single-use nudges from local events instead of an upfront tour. | Wire reviewed triggers to dismissible accessible UI and add frequency/disable controls. |
| 149 | Foundation | [`DaySevenRecap`](../crates/vbuff-core/src/onboarding.rs) derives a local-only milestone summary without telemetry or clip content. | Persist opt-in counters, show the recap once, and design an explicit redacted share artifact. |
| 150 | Runtime | `vbuff verify --file <path> --sha256 <hex>` streams offline verification; release CI publishes checksums/SBOM/provenance and the Trust surface exposes release-trust status. | Publish signed release metadata/keys and add provenance verification, not only caller-supplied checksum comparison. |

## Three review passes

| Pass | Focus | Corrections made before acceptance |
|---|---|---|
| 1 | Contracts, bounds, and failure behavior | Added strict parsers, one-shot callback replay defense, downgrade/key-rotation checks, tamper tests, bounded migration/export evidence, and explicit unknown SLO states; fixed every compilation/test mismatch before UI work. |
| 2 | Visual design and real interaction | Reworked the popup into compact History/Trust surfaces, replaced text actions with fixed icon controls where appropriate, centered and stabilized the empty-state seed picker, and regenerated/inspected 12 theme/DPI golden images after each correction. |
| 3 | Security, release, SOLID/DRY, and claims | Refreshed all 100 research repositories, split updater contracts into `vbuff-update`, and added clean-install/reproducibility/notarization/mutation/supply-chain gates. The final review also replaced callback and format-policy ambiguity with fail-closed contracts, bound release key ids into signatures, fixed the CF_HTML grammar, replaced JSON vault framing with a bounded binary envelope, removed duplicate conflict identity, made SLO defaults evidence-based, bounded CLI/tripwire state, and rejected incompatible monetization mechanisms in one decision record. |

## Acceptance gate

This batch is accepted only after formatting, strict clippy, all workspace and no-default tests, golden snapshots, release performance budgets, dependency policy/audit/vet, fuzz-target compilation, documentation contracts, local-link checks, workflow syntax review, and `git diff --check` pass. Native-required and foundation rows are not release claims.

Local macOS execution completed those gates on 2026-07-18, including five short libFuzzer campaigns and a generated app-bundle smoke test. The checked-in GitHub Actions matrix remains responsible for clean macOS/Windows/Linux reruns, credentialed notarization, independent-runner reproducibility, scheduled mutation testing, and longer fuzz campaigns.

The first post-merge Windows matrix run exposed an invalid cross-platform assumption: the native global-hotkey manager is thread-affine there, while the shared trait required `Send`. The follow-up review removed that unnecessary supertrait, documented event-loop ownership, and added a non-`Send` conformance test so the Windows contract cannot regress silently. The follow-up Windows lane also required target-gating the macOS/Linux-only `PathBuf` autostart import under strict clippy.
