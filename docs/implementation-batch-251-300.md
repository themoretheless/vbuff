# Implementation batch 251-300

Reviewed on 2026-07-21. This ledger is the execution overlay for backlog items 251-300 in [ideas-top-300.md](ideas-top-300.md). It separates resident behavior from bounded workflow/device/integration contracts and from evidence that still requires native clients, real transports, credentials, or maintainers.

## Status vocabulary

| Status | Meaning |
|---|---|
| **Runtime** | Exercised by the current resident binary, popup, store, CLI, or repository automation. |
| **Foundation** | Implemented and tested as a bounded reusable contract, but not connected to the final runtime surface. |
| **Adapted** | The proposal was narrowed to preserve privacy, accessibility, or a truthful product boundary. |
| **Native required** | Completion depends on real per-OS APIs, clients, transports, credentials, or assistive-technology evidence. |
| **Rejected** | The mechanism conflicts with a safety or correctness constraint; its replacement is recorded. |

## Item ledger

| Item | Status | Landed evidence | Remaining product work |
|---:|---|---|---|
| 251 | Runtime | [`AutoPauseController`](../crates/vbuff-platform/src/lifecycle.rs) resolves idle, lock, and remote signals with manual-pause precedence; startup remote-session detection pauses capture and records a typed reason. | Wire continuous native idle/lock/remote events on every OS and prove no capture occurs through transitions. |
| 252 | Foundation | [`RuleSuggestionEngine`](../crates/vbuff-core/src/workflow/everyday.rs) emits one bounded, redacted suggestion after repeated per-app actions and remembers dismissal in process. | Persist local counters/dismissals and add confirm/edit UX without collecting clipboard content telemetry. |
| 253 | Runtime | Capture health and size pressure publish typed, content-free one-shot alerts with direct Trust/Settings navigation and explicit dismissal. | Add native permission repair deep links and debounce evidence across permission/compositor transitions. |
| 254 | Foundation | [`HistoryScope::from_jump_key`](../crates/vbuff-gui/src/experience.rs) maps bounded single keys to URL, image, code, file, color, and snippet scopes. | Bind the keys only when the query is empty, preserve text entry/IME behavior, and add layout-independent input tests. |
| 255 | Runtime | Popup scope survives hide/reopen; the first Escape clears query/scope and a visible clear icon resets scope, while a second Escape hides. | Persist scope across process restart only after an explicit preference decision. |
| 256 | Runtime | Casual, Developer, and Privacy Max presets use typed [`ProfileDefaults`](../crates/vbuff-core/src/onboarding.rs); choosing one opens a review modal with the target values and a lower-history-cap warning, and mutates config only after explicit Apply. | Add first-launch profile presentation and a current-versus-target field diff for users with existing custom settings. |
| 257 | Runtime | [`clipboard_health_digest`](../crates/vbuff-store/src/lib.rs) derives DB bytes, item/largest/expiring/sensitive/suggested/stale counts from metadata using the same creation-time stale boundary as the visible review list; Settings shows it only when opted in. | Add optional scheduling and a last-shown marker without notifications or telemetry by default. |
| 258 | Foundation | [`group_copy_bursts`](../crates/vbuff-core/src/workflow/everyday.rs) groups bounded, distinct, time-adjacent clips from one source while retaining ordered IDs. | Add an accessible expandable row and paged store query without hiding or deleting individual clips. |
| 259 | Runtime | A per-connection SQLite `TEMP session_protected` set exempts selected clips from expiry, automatic clear, count eviction, and retention candidates; the action flyout exposes the state. | Include native clear-on-lock policy once lock events are live; explicit user delete remains authoritative. |
| 260 | Runtime | [`expiry_label`](../crates/vbuff-core/src/workflow/everyday.rs) supplies expired/today/day-count/kept/permanent labels used in row metadata. | Localize calendar boundaries and prove behavior across timezone/DST changes. |
| 261 | Runtime | The row action creates a new plain-text-only clip through [`plain_text_clone`](../crates/vbuff-core/src/workflow/everyday.rs), preserving original bytes and sensitive/local-only policy. | Add a confirmation when no lossless text representation exists instead of silently omitting the action. |
| 262 | Foundation | Shared [`clean_link`](../crates/vbuff-core/src/workflow/links.rs) powers plugin, popup preview, and browser contracts; `CleanLinkMemory` proposes a domain rule after repetition. | Persist confirmed domain policy and expose review/dismiss/reset UX. |
| 263 | Runtime | Capture soft/hard limits publish typed preview-only or skipped byte-budget alerts; existing pressure policy remains the single admission path. | Add an explicit one-time override that cannot bypass the hard cap or sensitive policy. |
| 264 | Runtime | The History filter row derives at most 16 recent source apps by capture time, independently of pin/presentation order, and renders direct source chips beside the kind selector. | Add keyboard focus mnemonics and native source identity where the backend can prove it. |
| 265 | Runtime | Settings computes bounded stale-pin candidates and provides keep-by-inaction, unpin, and confirmed delete controls. | Add review cadence/last-reviewed persistence and archive only after archive ownership exists. |
| 266 | Foundation | [`DeviceTrustLevel`](../crates/vbuff-sync/src/device_experience.rs) models full, receive-only, send-only, and untrusted authority with validation that forbids live writes from untrusted devices. | Persist verified device identity and enforce the policy in authenticated transport/daemon dispatch. |
| 267 | Foundation | `SyncItemSummary::local_only_by_default` rejects every sensitive or sync-ineligible item from rehearsal, replay, and estimates. | Prove every live replication ingress/egress path uses the same fail-closed decision. |
| 268 | Foundation | `rehearse_pairing` reports bounded transferable/local-only/metadata-first counts and bytes without materializing payloads. | Build pairing UI against real discovery and compare rehearsal with committed transfer receipts. |
| 269 | Foundation | `plan_selective_replay` accepts explicit item/collection selection, caps output, and skips local-only clips. | Connect to durable collections and an acknowledged retrying outbox. |
| 270 | Foundation | `sealed_revocation_tombstones` hashes item/device identities and encrypts authenticated tombstones to the target key. | Persist epochs, propagate through live peers, and require wipe acknowledgements before claiming revoke completion. |
| 271 | Foundation | `conflict_timeline` returns ordered content-free points and the existing deterministic resolver's winner/reason. | Add encrypted version persistence and a GUI that can reveal content only after local unlock. |
| 272 | Foundation | `SyncOutbox` owns bounded queued/retry/delivered state with redacted target and error diagnostics and rejects invalid or terminal-state transitions. | Drive it from a durable transport queue and surface actionable retry/network state. |
| 273 | Foundation | `transfer_materialization` resolves denied, full, metadata-first, or on-demand payload behavior from validated per-device and per-item policy. | Implement thumbnail/blob fetch and cancellation over measured mobile/hotspot transport. |
| 274 | Foundation | Per-device retention validates 1-3650 days and computes deterministic deadlines while supporting masked sensitive history. | Enforce deletion on the receiving device and collect signed retention receipts. |
| 275 | Foundation | `TravelMode` derives a temporary deny-sync, hide-sensitive, shortened-retention posture without rewriting the base device policy. | Add explicit start/expiry/exit UI and native lock integration; never auto-enable from location data. |
| 276 | Foundation | `QrHandoffToken` issues a one-time, expiring, URL-safe token whose QR payload contains no clipboard content. | Bind the token to authenticated local transfer and verified consumption; no clip is transferred today. |
| 277 | Foundation | `nearby_send_targets` returns only unique, verified, eligible devices seen within a bounded recency window, with redacted diagnostics and a strict result cap. | Implement privacy-preserving discovery and a native send sheet with wrong-target confirmation. |
| 278 | Foundation | `estimate_sync` reports bounded item/byte/sensitive/device totals under the same local-only and trust decisions. | Compare estimate to real transfer receipts and explain exclusions in UI. |
| 279 | Foundation | `allows_live_clipboard_write` separates history receipt from remote writes and denies untrusted devices. | Enforce at the native clipboard boundary and require visible target-device consent. |
| 280 | Foundation | Constructor-controlled, serialize-only `SharedSnippetProposal` state requires a non-author approval quorum, exact base revision, and approved state before commit. | Add an authenticated approval envelope, durable team identity/history, roles, and conflict UI before accepting wire state. |
| 281 | Foundation | [`BrowserSourceReport`](../crates/vbuff-ipc/src/integration/browser.rs) validates bounded origin/private-tab/link metadata and redacts URLs from diagnostics. | Ship a signed extension and authenticated local daemon bridge; unknown/private context must continue to fail closed. |
| 282 | Foundation | `CleanLinkRequest` requires an explicit user gesture and confirmed non-private context before calling the shared URL cleaner. | Add browser context-menu adapters and permission/install review. |
| 283 | Foundation | [`adapt_text_for_editor`](../crates/vbuff-ipc/src/integration/editor.rs) preserves CRLF/LF, handles Markdown fences, and applies bounded indentation by destination kind. | Run VS Code/JetBrains integration corpora and expose an immutable preview before paste. |
| 284 | Foundation | `EditorCaptureMetadata` adds language/file/repository/branch fields with strict bounds and path/project redaction in `Debug`. | Authenticate editor clients, define storage/search consent, and avoid collecting metadata outside explicit capture. |
| 285 | Foundation | [`VimRegisterRequest`](../crates/vbuff-ipc/src/integration/automation/snippets.rs) models the fixed `vbuff` namespace, a bounded slot, and explicit read-history/add-yank actions without carrying clip content. | Implement a Neovim client and daemon query route with explicit local authorization. |
| 286 | Foundation | One-shot `RemotePasteLease` authenticates recipient/session/content hash/expiry with HMAC and a bounded replay window. | Carry it through an existing authenticated SSH channel and require remote-shell confirmation; no remote endpoint exists. |
| 287 | Foundation | Typed automation commands cover get/add/paste/send with bounded tags and recipients; `TargetedSendRequest` separately binds bounded clip/device IDs to an expiry. | Map to reviewed Shortcuts/Tasker actions and exact platform consent. |
| 288 | Foundation | Existing scoped API policy combines with expiring capability tokens; webhook events add HMAC body/time/id binding and replay rejection. | Expose no listener until daemon authentication, revocation, rate limits, and audit UI pass. |
| 289 | Foundation | Constructor-controlled, serialize-only share ingress state validates bounded destination/tag input before preview and requires a preview-to-commit transition. | Build native share-sheet adapters and a compact accessible prompt; deserialize only an unprivileged wire DTO. |
| 290 | Foundation | `plan_snippet_mirror` computes bounded upsert/delete records from content hashes without logging expansion text. | Implement reviewed Espanso/TextExpander serializers, atomic output, and conflict/rollback UX. |
| 291 | Foundation | [`McpSessionLease`](../crates/vbuff-ipc/src/integration/access.rs) requires explicit consent, an exact read-only AI-shareable policy, expiry, and an HMAC proof bound to the session ID and policy hash. | Host a local MCP server only after authenticated daemon dispatch, revoke UI, session-key custody, and prompt-injection/privacy review. |
| 292 | Foundation | [`AdapterLimits`](../crates/vbuff-plugin/src/adapter.rs) bounds input/output, records, flavors, aggregate bytes, MIME, format IDs, and redacted adapter results; only realized inline bytes are accepted, and any declared integrity hash must match. | Run community adapters inside the future sandbox and add transactional import preview/rollback. |
| 293 | Foundation | [`rank_launcher_candidates`](../crates/vbuff-ipc/src/integration/query.rs) provides deterministic bounded ranking for Raycast, Alfred, rofi, dmenu, and fzf requests. | Add authenticated local dispatch and native client packages; no public endpoint exists. |
| 294 | Foundation | [`Osc52Observation::from_metadata`](../crates/vbuff-ipc/src/integration/terminal.rs) caps raw payload size, validates session/host metadata, hashes payload and origin identity, and pairs with policy that denies remote capture by default. | Integrate terminal adapters and distinguish trustworthy local/SSH/tmux provenance without placing clipboard payloads in logs. |
| 295 | Foundation | [`WebhookSigner`](../crates/vbuff-ipc/src/integration/webhook.rs) binds and verifies the expected endpoint id, monotonic event id, timestamp, body hash, and HMAC; `WebhookReplayWindow` rejects duplicate/old events while retaining the maximum observed validity horizon. | Add endpoint persistence, key rotation, delivery queue, TLS policy, and user-visible revocation. |
| 296 | Runtime | Versioned [public limitation ledger](limitations.md) records severity, current behavior, usable workaround, and evidence required to close each known gap. | Link it from every release and update version/IDs whenever limitations change. |
| 297 | Runtime | [`release-provenance.yml`](../.github/workflows/release-provenance.yml) assembles source-bound three-OS tests, scoped residue-canary logs, audit/deny/vet, measured budgets, CycloneDX SBOMs, reproducibility, checksums, binaries, and generated provenance into one manifested evidence artifact. | Verify a real tag artifact independently; whole-DB encryption remains explicitly unproven until SQLCipher lands. |
| 298 | Runtime | `vbuff config migrate preview/apply` reports schema-level changes, refuses silent schema-1 save, preserves comments and unknown fields, creates an owner-only rollback copy, and atomically writes schema 2. | Add future migrations as explicit ordered steps and prove crash/rollback behavior with filesystem fault injection. |
| 299 | Runtime | [Maintainer handoff playbook](maintainer-handoff.md) covers credential custody, normal/emergency release, dependency cadence, sunset, and a twice-yearly clean-room drill without placing secrets in git. | Assign two real custodians and complete the first recorded drill; documentation cannot prove access. |
| 300 | Runtime | [Scope review](scope-review.md) records Q3 dispositions/cut rules; [`quarterly-scope-review.yml`](../.github/workflows/quarterly-scope-review.yml) opens one deduplicated reminder each quarter. | Complete each review with named maintainers and update the checked-in decision record. |

## Three review passes

| Pass | Focus | Corrections made before acceptance |
|---|---|---|
| 1 | Bounds, privacy, and failure behavior | Added hard collection/device/payload/replay caps, identifier and nonzero-hash validation, redacted diagnostics, one-shot constant-time token checks, HMAC-bound leases, maximum-horizon replay retention, constructor-controlled privileged states, terminal-state guards, and fail-closed clean-link/adapter/transfer decisions. |
| 2 | Visual design, accessibility, and responsive behavior | Replaced one-click profile mutation with review/apply confirmation, kept stale-pin review independent from digest opt-in, removed contradictory expiry/protection labels, fixed kind-reset and source-recency behavior, eliminated coordinate-driven fixture state, and expanded the deterministic matrix to six states across theme and DPI. |
| 3 | SOLID/DRY, migrations, operations, and claim accuracy | Split automation and device experience into focused modules, centralized integration errors, aligned digest/list time semantics, preserved TOML comments/unknown fields, strengthened quarterly reminder deduplication, fixed the evidence bundle to contain its claimed binary/SBOM/provenance inputs, and corrected every API/evidence claim above against the implementation. |

## Acceptance gate

The active baseline requires formatting, strict all-target/all-feature clippy, locked workspace tests, root no-default-feature tests, release performance budgets, exact Linux native GUI goldens, dependency deny/audit/vet, fuzz-target compilation, documentation contracts/local links, workflow syntax review, three recorded review passes, and `git diff --check`. Native idle/lock events, live sync/clients/transports, hosted integrations/plugins, SQLCipher/OS-keystore proof, real assistive technology, and completed maintainer drills remain separate gates.
