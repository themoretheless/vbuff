# SOLID/DRY-ревизия vbuff — сводный отчёт

Дата: 2026-07-26. База: HEAD `eaa4d26` (после волн top-10). Метод: 8 зональных explore-агентов + сквозная DRY-ревизия; мёртвый код и устаревшие аудиты как находки не засчитывались, кроме случаев, где он создаёт двойной источник правды.

## Сквозные темы (дедуплицировано между зонами)

### T1. Три грамматики поиска, два движка — блокер «единого поиска»
- `core/recall/query.rs:148-180` (фасеты `app|kind|tag|device|before|after`, кавычки, относительные даты) vs `store/search.rs:38-50` (`host|color|lang|iso_date`, без кавычек) — словари не пересекаются, каждый парсер молча пожирает чужие фасеты.
- Исполнителей три: `core/filter.rs:25` (мёртв, doc-comment врёт), живой in-memory `gui/projection.rs:35`, спящий SQL `Store::search` (lib.rs:1521; потребитель — только `src/ask.rs`).
- Expiry-фильтр — 3 копии (`filter.rs:33-39`, `projection.rs:87-91`, `store/lib.rs:1551`), одна с carve-out `session_protected`; `workflow/everyday.rs:195-218` — мёртвый in-memory двойник той же концепции.
- Добавление фасета = правка ~8 точек в 3 файлах (query.rs:26-33/60-63/164-178/284-311, recall/search.rs:146-164/276-321, store/search.rs:44). Реальный баг уже есть: `app:` с `to_ascii_lowercase` (query.rs:190) не матчит не-ASCII `source_app`, нормализованный юникодно (recall/search.rs:121-126).
- **Решение:** единый query AST + табличный реестр `FacetSpec` в vbuff-core; store маппит AST→SQL; `store/search.rs::parse_query`, `SearchSession`, `filter::search`, `everyday::SessionProtection` удалить; `has_payment_number` — в грамматику или перестать писать. **L** (реестр+AST — M).

### T2. Валидация идентификаторов/меток: ~20 копий в 6 крейтах
Один предикат (`non-empty && len<=max && [A-Za-z0-9._-]`): core/trust ×4, ipc ×5+ (включая дубль в одном модульном дереве automation.rs:121/129 и access.rs:277, инвертированную полярность terminal.rs:152), sync, store/data_lifecycle.rs:1163, plugin ×6 (включая verbatim `valid_version` дважды в одном крейте), update — два `validate_key_id` в одном крейте. Парная `valid_label` — 4 копии. Блокер sync-транспорта: расхождение charset = reject-штормы между устройствами.
- **Решение:** `vbuff-types::validation::{is_valid_identifier, is_valid_label, valid_key_id, valid_version, all_zero}` (pure, serde-only). **M, механический.**

### T3. Кодек `ContentKind ↔ int`: 3-4 копии с разной семантикой ошибок
`store/lib.rs:3093-3116` (wildcard → `Other`, молчаливая деградация) vs `store/lifecycle.rs:342-369` (`Err(Corrupt)`); + `cas.rs:357-380` (`kind_slug`/`ALL_KINDS` — двойная правда в одном файле); + recall `kind_code`/`natural_kind`; + SQL-литералы `kind = 7` (lib.rs:652,668,786,799,1894,1909). Новый вариант компилятор в lib.rs не поймает (wildcard).
- **Решение:** `ContentKind::{to,from}_stored_discriminant` (fail-closed) в vbuff-types + `FromStr`; SQL параметризовать. **S.**

### T4. Enum-mirrors на границах крейтов (types уже в зависимостях)
`CapabilityLevel/Severity/FeatureCapability` ≡ `CapabilityView*` (ручные маппинги main.rs:220-241, app.rs:202-206; дрейф уже есть — serde default только у View); `PrivacyDecisionKind` ≡ `PrivacyDecisionLevel` (diagnostics.rs:176-178); `SelectionSource` ≡ `ClipboardSelection` (capture.rs:416-419); security-posture словарь разъехался между GUI-шапкой и Trust-поверхностью (app.rs:3079-3109 vs trust_view.rs:29-33).
- **Решение:** types владеет словарём, platform/core публикуют его напрямую или `impl From`; маппинги в бинарнике исчезают. **S–M.**

### T5. Крипто-примитивы скопированы вместо выделения общего слоя
- 6 HMAC-механизмов: построение MAC написано дважды (sign/verify) в каждом — webhook.rs:111-130, remote.rs:144-177, access.rs:234-264, callback.rs:229-268, api_token.rs:127-173, sync/capability.rs:31-86; фрейминг разъехался (`v1.` префикс не везде).
- 3 подписанных hash-chain (membership/provenance/ledger) с разошедшейся защитой: bound и валидация signer есть только у membership; подписи `[u8;64]` vs `Vec<u8>`.
- Вывод подписывающего ключа membership продублирован в append и verify (membership.rs:109-127 vs 187-205) — дрейф = обход подписи.
- 3 копии signing-preimage в update (manifest.rs:387-397, attestation.rs:79-89, manifest.rs:80-93) + плавающая конвенция NUL-терминатора доменов по workspace (9 билдеров).
- **Решение:** `hmac_proof(domain,key,parts)`; generic `SignedChain<E>` с хуком валидации и bound для всех; `expected_signing_key` — одна функция; `signing_preimage` в update + письменная доменная конвенция. **2×M + S.**
- Отдельно: callback replay-окно (`callback.rs:154`) не чистится никогда — единственное из четырёх; утечка в долгоживущем процессе. **S, фикс.**

### T6. Приватный домен: определение секрета и sensitive-инвариант размазаны
- Две OTP-эвристики (policy.rs:417-431 vs secret.rs:31-55) и три порога секретности: `secret_threshold` (policy), литерал `0.9` (store clawback lib.rs:2008-2010), без порога (plugin migration.rs:80).
- Sensitive-кламп (sync_eligible/ai_allowed/TTL) — 3 копии (lib.rs:1003-1015, data_lifecycle.rs:1226-1236, lib.rs:2014-2018); scrub-SQL — 2 verbatim-копии списка колонок (lib.rs:754-770, 2021-2030); `SecretHandling.{mask,sync_allowed}` никто не читает — ложная ручка.
- `CapturedClipboard::default()` — fail-open булевы `coherent_generation/primary_intended` (traits.rs:80-94): БАГ-2 закрыл concealment/provenance, эти два остались. Плюс два канала evidence (статический `evidence()` vs per-read поля) — нативный бэкенд легко рассинхронизирует.
- **Решение:** секретный домен целиком в secret.rs (OTP как SecretKind, общий `min_actionable_confidence`); `tighten_sensitive()` + `SENSITIVE_SCRUB_SET` в store; три-стейты для generation/intent; один канал evidence. **M + S + S + M.**

### T7. Мёртвые файлы-двойники с ложной правдой (удалить первым проходом)
- store: `row.rs`, `paths.rs`, `schema.rs` (не подключены; SCHEMA_VERSION=1 при живой v7; row.rs без metadata_json).
- gui: `render.rs`, `theme.rs`, `color.rs`, `input.rs` (~290 строк; color.rs хранит исправленный баг parse_hex_color; render.rs не компилируется — `UiAction::ClearAll`).
- src: `gui.rs`, `actions.rs`, `constants.rs` (actions.rs — paste-путь в обход ledger/sensitive/guard!).
- core: `trust/rules.rs` (коллизия имён `CaptureRule`/`PasteGuardDecision` с живыми), `security_audit.rs` (HMAC-цепочка без эмиттеров), `eviction.rs` (doc-comment enforce_cap врёт о parity-тесте; политики разошлись), `filter.rs`, trust/access.rs, хвосты posture/consent, `boards::ActionCandidate` (коллизия с живым в plugin).
- platform: `keymap.rs` (рассинхронизированная «каноническая» спецификация), `tripwire.rs` (несбыточный контракт), мёртвый алиас AutoPauseReason; коллизия `WrappedKey` (security.rs:33 vs sync/crypto.rs:14).
- **S суммарно, доходность на единицу риска — максимальная.**

### T8. Store: граница чтение/запись и проекции
- `count()` делает `purge_expired` DELETE (lib.rs:2598-2604) → цепочки из insert/enforce_cap/search_page; вооружённая ловушка для read-only открытия (doctor). **S.**
- 10 копий 11-колоночной проекции клипа + ручная гидрация (collect_clips внедрён лишь в 2 местах). **M.**
- 5 дивергентных путей удаления с разной семантикой legal-hold на краевых случаях (delete/apply_batch/delete_with_grace/audit_content_hashes без hold-проверки/массовые с 24 копиями предикатов). **M.**
- `Store` 74 публичных метода — НЕ дробить на трейты (школьничество; History-фасад уже сужает). Достаточно секций impl.

### T9. src/: двойные правды вокруг команд и конфига
- `AppCommand` (commands.rs) — дубль `UiAction` включая redacted Debug ×2: забудешь второй — утечка контента в логи. **M.**
- Config↔ShareableConfig: 6-7 точек маппинга на ключ; строково-типизированные enum-ключи ×3 (нет serde-derive на DensityMode/HandedMode/UnknownEvidencePolicy). **M + M.**
- Бэкенд захардкожен: `ArboardClipboard::new()` в capture.rs:194 и paste.rs:38 — прямой блокер нативных бэкендов. **M (factory).**
- Два определения security-вердикта (main.rs:243-272 vs doctor.rs:29-31); три детекта сессии с тремя источниками (lifecycle/capabilities/gui) — doctor и GUI уже могут разойтись. **M + S–M.**
- `paste_modifier` валидируется и персистит, но ни на что не влияет — лживая ручка (удалить из CONFIG_KEYS до оживления injection). **S.**

### T10. GUI: покадровые пересчёты и монолит
- Нет мемоизации: `filtered()`+токенизация near-dup, `clip_by_id`, `complete_query` ×2, `recent_source_apps`, `contextual_search_hint` — каждый кадр, принудительный repaint 1/с. `AppState::revision` уже есть, но не используется. → `ProjectionCache`. **M, до утяжеления поиска.**
- `PopupApp` 37 полей, `ui()` ~666 строк, 24-элементный tuple-снапшот, `render_row` 276 строк/13 аргументов. Пошагово: snapshot-struct → handle_history_keys → RowContext → target: Option<ClipId> в палитре (сейчас хак с подменой self.selected) → вынос поверхностей по образцу trust_view.rs. **Серия S/M.**
- Design-система игнорируется: 4 баннера копипастой, ad-hoc отступы/ширины/цвета мимо токенов, 6 формулировок одного sensitive-отказа, третий оттенок красного. **Серия S.**

### T11. Update-канал
`VerifierState` без `schema`-версии и integrity (state.rs:19-23) — добавить поле сейчас, пока формат молод; связка verify+store, чтобы долговечность не была opt-in. **S/M.**

## План волн

**Волна A — «Снять мины» — ЗАКРЫТА 2026-07-31** (коммиты `04c9768`, `21823e3`, `0480cd5`):
1. ✅ Файлы-сироты удалены во всех пяти зонах (T7); ссылки в docs/implementation-batch-* на удалённые файлы переписаны в «Retired».
2. ✅ `vbuff-types::validation` + миграция копий в ipc/sync/store/core/plugin/update (T2). Обследование зафиксировало, что «core/trust ×4» из ревью не подтверждается: в trust одна копия, остальные жили в recall/feedback/intelligence.
3. ✅ `ContentKind::{stored_discriminant, from_stored_discriminant, slug, FromStr}` в types (T3); store перешёл на fail-closed декод (было молчаливое `_ => Other`), литерал `kind = 7` получил один источник и пин-тест на текст триггера. `recall::kind_code` намеренно оставлен локальным доменом фингерпринта (не персистится) с doc-comment.
4. ✅ `tighten_sensitive` + `SENSITIVE_SCRUB_SET` (T6-store); `count()` больше не удаляет (T8). Побочно закрыты два расхождения в сторону ужесточения: clawback больше не растягивает более короткий TTL, импорт получил тот же потолок TTL.
5. ✅ callback replay-окно ограничено по времени и числу записей + монотонный пол часов (T5); `VerifierState` получил версию схемы, integrity-checksum и связку verify+store (T11); три signing-preimage в update слиты в одну функцию с пин-тестами байтов (T5). Доменная конвенция описана в [domain-separation-convention.md](domain-separation-convention.md).
6. ✅ `paste_modifier` убран из модели (ключ TOML остаётся принимаемым, чтобы существующие конфиги грузились); `SecurityPosture::level()`/`summary()` — единый вердикт для GUI и doctor; зеркала `CapabilityLevel/Severity/FeatureCapability`, `PrivacyDecisionKind`, `SelectionSource` заменены на типы из vbuff-types (T4, T9).

**Найдено сверх плана и исправлено:** аудит доменных строк (42 билдера, а не 9) выявил реальную коллизию в `sync/merkle.rs::leaf_hash` — два поля переменной длины без длин-префиксов, идентификаторы от пира не валидируются; два разных набора записей давали один корень, и расхождение навсегда пряталось от реконсиляции. Исправлено длин-префиксами с бампом домена до `v2` (потребителей вне модуля не было) + регрессионный тест на конкретную коллизию.

**Проверено и отклонено:** заявка того же аудита о расхождении `content_hash` / `content_hash_from_flavors` на spilled-флейворах не является живым дефектом — все пути гидратируют клип до пересчёта хеша, а `content_hash` вообще не имеет продакшн-вызовов.

**Волна B — «Единые примитивы» (M, ~1-1.5 недели):**
1. ✅ **Сделано 2026-08-01.** Query AST + реестр `FacetSpec` + `normalize_lookup` в `core/recall/grammar.rs` (пока не подключён к живым путям — подключение это волна C пункт 1); характеризационные голдены на обе грамматики (28 тестов в core, 23 в store); карта поверхностей в [search-grammar-audit.md](search-grammar-audit.md).

   Аудит поправил три факта самого ревью: грамматик не три, а пять (кроме двух текстовых живут `HistoryScope`, мёртвый `SearchScopeLock` и третий список префиксов в `complete_query`); копий expiry-фильтра не три, а девять; единственный продакшн-потребитель `Store::search` — не `src/ask.rs`, а `local_similarity_search_with`, который использует его как лексический пре-фильтр и выбрасывает его ранжирование. Стоимость нового фасета — 13 точек правки в 4 файлах для паритета в GUI (до 23 с SQL), из них компилятор ловит одну.

   Подтверждено исполняемыми тестами и НЕ исправлено (это отдельные решения, а не рефакторинг):
   - `app:` не матчит не-ASCII имя приложения, набранное так, как оно показано в UI (парсер лоуеркейсит только ASCII, матчер — юникодно). `device:` имеет зеркальный дефект: работает точное написание и не работает строчное, то есть два соседних фасета требуют противоположного ввода. `normalize_lookup` из нового модуля закрывает оба, но только после миграции.
   - `tag:` в GUI не матчит **никогда**: контекст всегда строится с `tags: None`, а `ClipTags` не конструируется нигде в продакшн-коде. Фасет при этом есть в грамматике, в автодополнении и в подписях матчей.
   - Чужой фасет не игнорируется, а превращает весь текстовый терм в мёртвый литерал: `release notes host:example.com` не находит клип, где эта фраза есть буквально.
   - `iso_date` со временем и unicode-хосты недостижимы в принципе: индексируются в одном виде (дословно / punycode), ищутся в другом.
   - Порог `FTS_ROW_THRESHOLD` меняет **семантику**, а не скорость: LIKE в SQLite фолдит регистр только для ASCII, `unicode61` — весь, поэтому один и тот же не-ASCII запрос даёт разные результаты при 249 и 250 строках истории.
   - `has_payment_number` пишется в `clip_facets`, но не входит ни в одну грамматику и не читается ничем.
   - Тест `malformed_structured_query_fails_closed` закрепляет несуществующее поведение: `unknown:needle` парсится успешно и уходит в текст, а тест проходит лишь потому, что подстрока не встречается в фикстурах.
   - Carve-out `session_protected` в `search_page` иллюзорен: `purge_expired` удаляет такие строки безусловно и вызывается на каждом `insert`.
2. ✅ **Сделано 2026-08-01.** `hmac_proof` (`vbuff-types::mac`), `SignedChain` (`vbuff-sync::chain`), `ReplayGuard` (`vbuff-types::replay`); мигрированы все 6 HMAC-механизмов, 3 подписанные цепочки и 4 replay-окна. Независимый ревьюер проверил результат: [crypto-migration-review.md](crypto-migration-review.md).

   Байты пяти персистентных/внешних HMAC сохранены намеренно (webhook проверяют сторонние получатели вне репозитория), и совместимость доказана тестовым оракулом, дословно воспроизводящим прежнюю ручную последовательность, а не только пин-тестами. Четыре формата сломаны осознанно с бампом домена (ledger v1→v2, membership v2→v3, SAS v2→v3, custody v1→v2) — у всех четырёх нет ни одного пишущего вызова вне собственного крейта, поэтому разрыв сегодня бесплатен, а старые данные fail-closed не десериализуются.

   Закрытые дефекты, которых ревью не называло:
   - provenance-цепочка не включала подписанта в подписываемые байты вообще, а лишь сравнивала его постфактум; у ledger и provenance не было ни границы числа записей, ни проверки автора при записи.
   - `RemoteReplayWindow` и `WebhookReplayWindow` не имели монотонного пола часов: откат системных часов воскрешал уже сожжённые записи. Заявление ревью, что callback — «единственное неочищаемое из четырёх», устарело после волны A; настоящим расхождением был именно пол часов.
   - `capability.rs` — единственный HMAC в воркспейсе вообще без доменной сепарации, плюс два неограниченных replay-множества. Переведён на общий MAC (домен `v2`) и на `ReplayGuard`; ревокация теперь тоже fail-closed, а не молча теряется при переполнении.
   - SAS редуцировался по модулю 10^19 при выводе в 20 цифр, то есть первая цифра всегда была нулём: 63.1 бита вместо задокументированных 66.4, и один из двадцати сравниваемых людьми символов не нёс информации. Тест теперь проверяет, что ведущая цифра варьируется.
   - `WipeReceipt` получил домен, но не получил fail-closed guard: старая квитанция десериализовалась и падала как подделка, а не как устаревший формат. Добавлено поле `schema` (входит в подписываемые байты) и проверка до суждения о подписи.
   - Комментарий в `vbuff-types/Cargo.toml` утверждал, что `hmac`/`sha2` не добавляются ни в один граф сборки. Для vbuff-platform это неверно (у него vbuff-core только в dev-зависимостях) — комментарий исправлен на честный.

   Остаётся решением, а не дефектом: монотонный пол часов вводит режим постоянного отравления окна, если вызывающий передаст время из далёкого будущего (сейчас `now_ms` приходит от самого процесса, не от атакующего); `content_hash` по-прежнему без домена.
3. ✅ **Сделано 2026-08-01** (вместе с пунктом 7). Секретный домен целиком в `core/secret.rs`; две OTP-эвристики слиты в объединение (не в пересечение), пороги разделены на два имени с одним домом.

   **Закрытая утечка.** Гейт захвата и детектор использовали РАЗНЫЕ списки OTP-маркеров: у гейта не было `"one-time"`.

   Первоначально эта запись утверждала, что `"Your one-time password is 483920"` захватывался несекретным при настройках по умолчанию. Независимая проверка это опровергла, и утверждение исправлено: у гейта было два пути, и второй (`detect_secrets`, включён по умолчанию) такой текст ловил. Реальный размер утечки уже: она проявлялась только при выключенном `detect_secrets`, когда OTP должен был оставаться защищённым в обход тумблера — то есть «verification code» маскировался, а «one-time password» нет. Зеркальная половина находки верна полностью и достижима по умолчанию: голый шестизначный код маскировался гейтом, но был невидим для реклассификации в store.

   **Закрытая потеря данных (найдена при работе, ревью её не называло).** Энтропийный детектор срабатывал на любой ASCII-токен 24-256 символов с тремя категориями символов — под это подходит обычный URL и windows-путь. Итог: пользователь копирует ссылку, она помечается секретной, исключается из синхронизации и **удаляется через 120 секунд**. Проверено исполняемым тестом до правки. Вместе с этим вскрылось второе: токенизатор не разбивал URL, поэтому `https://…?token=ghp_…` определялся только как `HighEntropy 0.72`, а структурный детектор токена не срабатывал вовсе — то есть настоящий секрет в ссылке не подлежал реклассификации (0.72 < 0.90). Исправлены обе стороны: сканирование частей локатора плюс отказ энтропии от структурных токенов; тесты пинят, что секрет внутри ссылки по-прежнему находится, а случайное значение в ней по-прежнему взвешивается.

   Пороги: `min_actionable_confidence(kind)` — вопрос «пропустить ли немаскированным» о ещё не сохранённом клипе (дёшево и обратимо при ошибке); `MIN_RECLASSIFY_CONFIDENCE` — вопрос «стереть ли проекции и назначить срок удаления клипу, с которым пользователь живёт неделями» (разрушительно и задним числом). Слияние было бы неверным в обе стороны.

   **Регрессия, внесённая и закрытая внутри этой же волны.** Расширение правила OTP до объединения сделало его видимым для реклассификации, а она запускается на каждом холостом тике и необратима. Проверено тестом на живом store: из четырёх обычных заметок (`error code 1024 while opening the project`, `discount code SAVE20 valid until 2026`, `area code 415 is San Francisco`, `verify the invoice 4821 before sending`) уничтожались три. Никакое лексическое правило не отличает «error code 1024» от «verification code 123456» — они структурно тождественны, разница только смысловая. Поэтому порога уверенности недостаточно: реклассификация теперь требует ещё и **структурного** свидетельства (`SecretKind::is_structural`) — заголовок приватного ключа, префикс `AKIA`, Luhn-валидный номер карты, три сегмента JWT не встречаются случайно, а английское слово рядом с цифрами встречается постоянно. Захват по-прежнему действует на оба вида свидетельств: замаскировать свежую копию стоит одного повторного копирования, а стереть сохранённую заметку стоит заметки.

   `SecretHandling.{mask, sync_allowed}` подтверждены мёртвыми и удалены: `sync_allowed` был константой `false`, притворявшейся решением, а `SecretMask` описывал словарь маскирования, которого не существует.

4. ✅ **Сделано 2026-08-01.** Детект сессии сведён к одному источнику: `SessionContext::current()` мемоизирует снимок процесса (`OnceLock`), приватный `from_environment` — единственное место, читающее env. `SecurityPosture::detect` больше не читает `WAYLAND_DISPLAY` сам, а принимает `&SessionContext`; `PasteCoordinator::system` — тоже; GUI перестал читать `XDG_SESSION_TYPE` и получает готовую метку через `PopupApp::set_session_environment`. `doctor` остался отдельным процессом со своим снимком (это и есть его смысл), но внутри себя тоже одним.

   Аудит уточнил формулировку самого ревью: расхождение между тремя детектами было не гонкой, а **разными вопросами к разным переменным**. Все поля `SessionContext` статичны по построению — они выводятся из environ, который ядро фиксирует на `exec` и после этого не обновляет (`set_var` в кодовой базе отсутствует), поэтому два чтения в одном процессе физически не могли дать разных ответов *на один и тот же вопрос*. Спрашивали же разное: lifecycle смотрел `WAYLAND_DISPLAY`, затем `DISPLAY`, затем `CI`, с приоритетом Wayland; capabilities — только `WAYLAND_DISPLAY` и **без** проверки target_os (на macOS/Windows с выставленной переменной в посту́ре появились бы Wayland-возможности при `display_server = mac_os`); GUI — `XDG_SESSION_TYPE`, третью переменную.

   Живое расхождение, а не гипотетическое: на macOS и Windows `XDG_SESSION_TYPE` не выставлен никогда, поэтому отчёт обратной связи из GUI на каждой такой машине писал `Session: unknown`, пока `doctor` в том же окружении печатал `"display_server": "mac_os"`. На Linux те же две переменные расходятся, когда X11-клиент запущен из Wayland-сессии со снятым `WAYLAND_DISPLAY` (`XDG_SESSION_TYPE=wayland`, а lifecycle по `DISPLAY` выбирает X11) и в обратную сторону при `XDG_SESSION_TYPE=tty` с поднятым вложенным X-сервером.

   Статическое против динамического разделено явно, а не заморожено целиком: `SessionContext` (display server, remote-запуск, seat, injection) — снимок; `SessionState`/`AutoPauseSignals`/`PowerSignals` (lock, foreground, sleep, idle, активное удалённое управление) — перечитываются на каждом наблюдении и не кэшируются нигде. Асимметрия вокруг «remote» зафиксирована в doc-comment: `SessionContext::remote` отвечает «процесс запущен внутри удалённой сессии» (фиксируется при старте), `AutoPauseSignals::remote_control_active` — «прямо сейчас кто-то управляет этим рабочим столом» (не фиксируется). Второе сегодня не подключено к продакшн-пути: `AutoPauseController` существует и покрыт тестами, но живых наблюдателей у него нет, так что VNC/RDP, подключившийся к уже работающему vbuff, авто-паузу не вызовет. Это отдельная работа, а не следствие этого пункта.
5. ✅ **Сделано 2026-08-01.** Точки правки на один общий ключ конфига: 7 → 4, из них три ловит компилятор. Заведены `UnknownEvidenceMode`/`UiDensity`/`UiHandedMode` с serde-derive: невалидное значение теперь отвергается при разборе с перечислением допустимых, а не отдельной проверкой, которую можно забыть для нового значения.

   `serde(flatten)` осознанно отвергнут, вопреки формулировке ревью: serde запрещает его вместе с `deny_unknown_fields`, на котором держится отказ от мусорных импортов, и он потребовал бы вложенности (`config.shared.hotkey`), ломающей доступ к полям по всему воркспейсу. Вместо него декларативный макрос порождает `ShareableConfig`, экспорт и импорт из одного списка полей — расхождение невозможно, поле, забытое в списке, остаётся локальным в обе стороны (fail-closed для приватности). Схема обмена осталась второй, старые экспорты читаются.

   Не сделано намеренно: `CONFIG_KEYS` остался списком вручную — избавиться от него можно либо `deny_unknown_fields` на `Config` (тогда перестанут грузиться конфиги с retired-ключами), либо разбором через `toml::Value` (теряются строка и столбец в ошибках). Это единственная оставшаяся точка, которую можно молча забыть, и она падает громко при загрузке.

6. ✅ **Сделано 2026-08-01.** `AppCommand` теперь **содержит** `UiAction`, а не зеркалит его (наборы вариантов не совпадают: у бинарника есть четыре команды трея и хоткея, которых GUI не порождает — искусственное объединение было бы хуже). Оба ручных `Debug`-импла удалены, редактирование содержимого живёт в новых типах `ClipText`/`RestoredClip` — по одному impl на тип, забыть ветку не в чем. Макрос порождает счётчик вариантов из самого списка, поэтому новый вариант роняет тест-страж дважды: сначала на неисчерпывающем match, потом на несовпадении числа образцов. Цепочка проверена экспериментом с временным вариантом, а не рассуждением.

   **Поправка к ревью:** фактического расхождения между двумя `Debug` на момент правки не было — оба редактировали одно и то же. Находка была верной оценкой риска, а не описанием живой утечки.

   Вторая половина: `ClipboardBackend` сведён к одному входу `write(flavors, &WriteOptions)` с fail-closed дефолтом, привязанным пин-тестом к прежнему поведению. `ClipboardRetention` стал лестницей из четырёх состояний, чтобы сохранить различие «никто не решал» против «явно согласились на историю ОС»; квитанция получила вариант «ничего не просили», иначе дефолтная запись утверждала бы применённый хинт там, где его не запрашивали. Побочно ужесточён путь секретной записи: он проходит только по положительной квитанции, а не по отсутствию отказа.
7. ✅ **Сделано 2026-08-01.** `GenerationCoherence{Coherent,Torn,Unknown}` и `SelectionIntent{Intended,Incidental,Unknown}` в `vbuff-types` (дефолт `Unknown`, как у `ConcealmentSignal`); `CapturedClipboard` и `CaptureInput` переведены, `CaptureForensicEvent` вместо пары взаимно отрицающих булевых (`owner_changed`/`coherent`) хранит одно трёхзначное поле.

   Решения в гейте различаются осознанно и записаны в doc-comment: неизвестная когерентность **не** отбрасывает захват в Guard/Allow (это вечное состояние arboard — отказ убил бы весь захват), а в режиме Skip даёт `coherence_unknown`, ровно как у concealment; неизвестный intent, наоборот, fail-closed во **всех** режимах — он читается только для PRIMARY, где отсутствие сигнала само по себе и есть опасность (выделение мышью), и сегодня не роняет ни одного захвата, потому что PRIMARY не отдаёт ни один бэкенд. Путь явного восстановления пропущенного клипа сравнивает `!= Torn`, а не `== Coherent`, иначе он молча отключился бы на arboard.

   Заодно удалён второй канал evidence: статический `ClipboardBackend::evidence()` и per-read поля могли расходиться, и бэкенд был способен утверждать статически одно, а на каждом чтении другое.

   Второй канал evidence убран целиком: `BackendEvidence`/`ClipboardBackend::evidence()` удалены, а стартовая проверка «источник недоказуем» в `src/capture.rs` теперь судит по `provenance_confidence` реального чтения. Статический канал не нёс ни одного факта сверх per-read полей (backend без наблюдения возвращает `Unknown` на каждом чтении), но позволял предупреждению в UI и решению гейта разойтись на одном и том же сигнале.

**Волна C — «Структура» (L, по готовности):**
1. Решение по поисковым стекам (RFC: GUI на search_page или store-поиск внутренним) + миграция (T1).
   RFC написан: [rfc-search-stack.md](rfc-search-stack.md) - три варианта (A: только SQL, B: только
   in-memory, C: оба на одном AST), матрица по 12 дефектам, список бэкфиллов, рекомендация и
   открытые вопросы. **Решение не принято, миграция не начата.**
2. GUI: ProjectionCache → расчленение PopupApp по шагам (T10).
3. Backend factory + WorkerState/poll_once — шаг (b) только при появлении второго бэкенда (T9).
4. Единый путь удаления + единая проекция клипа в store (T8) — перед sync-репликацией.

## Процессные правила против рецидива
- Новый enum на границе крейтов — сразу в vbuff-types.
- Новый идентификатор — через `types::validation`.
- Новый serde-контракт — `deny_unknown_fields` + осознанный `#[serde(default)]`.
- Чек-лист каждой волны: grep на mod-объявления новых файлов (сироты); sign/verify используют один MAC-билдер; redacted Debug — в одном месте на тип.
